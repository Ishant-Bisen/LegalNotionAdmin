# Force dev frontend to call the deployed backend directly.
VITE_API_BASE=https://api.legalnotion.in

# Optional local backend proxy (keep commented when testing Render):
# VITE_DEV_PROXY_TARGET=http://localhost:3000
